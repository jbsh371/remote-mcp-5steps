# 이 책이 확인한 조합입니다. 앞의 두 줄을 번호로 고정한 것은 재현 때문입니다 -
# fastmcp 만 고정하면 딸려 오는 mcp 판이 설치 시점에 따라 달라집니다.
# httpx 는 번호를 고정하지 않고 범위만 정해 둡니다.
fastmcp==4.0.3
mcp==2.1.1           # fastmcp 4.0.3 이 쓰는 기반 SDK. 2.2.0 에서도 점검은 통과했습니다
httpx>=0.27          # 최종 장 todo_adapter.py와 test_smoke.py가 씁니다 - fastmcp 4.0에 딸려 오지 않습니다
#
# 더 새 판으로 올리실 때는 위 두 줄의 번호만 바꾸고 test_smoke.py 를 돌려 보세요.
# 무엇이 먼저 흔들리는지는 책 부록 G 에 다섯 가지로 적어 두었습니다.
# 이전 세대에서 돌리실 거라면 fastmcp==3.4.7 로 바꾸고 mcp 줄은 지우세요.
# 그 조합(mcp 1.30.0)에서도 점검 53개가 전부 통과합니다.
